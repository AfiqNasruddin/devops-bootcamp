services:
  profile:
    image: redis
    volumes:
      - me:/data

  server:
    image: ghcr.io/infratify/arena-server

  avatar:
    image: ghcr.io/infratify/arena-avatar
    ports:
      - "8081:8080"
    environment:
      NAME: Afiq
      COLOR: grey
      SERVER: server:3000
    depends_on:
      - profile
      - server

volumes:
  me:
